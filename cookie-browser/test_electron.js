// Let's check if there's a way to access the internal module system
// to load the "electron" module

// Check if there's a way to access the internal module system
// to load the "electron" module
var Module = require("module");

// Check if there's a way to access the internal module system
// to load the "electron" module
var Module = require("module");
